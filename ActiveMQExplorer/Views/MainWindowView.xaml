<Window x:Class="ActiveMQExplorer.Views.MainWindowView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="MQ Explorer" Height="450" Width="800">
       <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem IsCheckable="true" Header="_Settings" Click="GoToSettings" >
                    <MenuItem.Icon>
                        <Image Source="/Resources/user.png" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        <TextBox AcceptsReturn="True" />
        <Label x:Name="label" Content="{Binding InfoText}" Height="401" VerticalAlignment="Top"/>
    </DockPanel>
</Window>
